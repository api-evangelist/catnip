generated: '2026-08-13'
method: probed
status: published
source: >-
  https://academy.chatfuel.com/llms.txt advertises the endpoint; the tool list below
  was read from a live anonymous POST of {"jsonrpc":"2.0","id":1,"method":"tools/list"}
  to https://academy.chatfuel.com/_mcp/server (HTTP 200, text/event-stream).
provider: Chatfuel
providerId: catnip
description: >-
  Chatfuel serves a remote MCP server on its own documentation host. It is a
  DOCUMENTATION-SEARCH server generated by the Fern docs platform, not a product
  API server: the single exposed tool performs retrieval over academy.chatfuel.com
  and returns doc passages with source URLs. It exposes no Chatfuel business
  operations (contacts, broadcasts, bots), and a call for "Broadcasting API"
  returned "does not include information about a Broadcasting API". Recorded as it
  really is: a real, reachable, unauthenticated agent surface with a documentation
  scope.
deployment:
  mode: remote
  endpoint: https://academy.chatfuel.com/_mcp/server
  auth: none
  verified: probed
server:
  name: academy.chatfuel.com docs
  transport: http
  protocol: MCP JSON-RPC 2.0 over HTTP with text/event-stream responses
  url: https://academy.chatfuel.com/_mcp/server
  platform: Fern (buildwithfern.com) generated docs MCP server
  scope: documentation-search
additional_servers:
- name: academy.chatfuel.com changelog
  url: https://academy.chatfuel.com/changelog/_mcp/server
  scope: documentation-search (changelog section)
  source: https://academy.chatfuel.com/changelog/llms.txt
  note: >-
    Section-scoped sibling advertised in the changelog llms.txt. Same platform and
    transport; not separately introspected.
tools:
- name: searchDocs
  title: Search academy.chatfuel.com Docs
  description: >-
    Search the documentation at https://academy.chatfuel.com. Returns relevant doc
    passages with source URLs.
  read_only: true
  input_schema:
    type: object
    properties:
      query:
        type: string
        description: The search query to run against the docs
    required: [query]
    additionalProperties: false
x-evidence:
  fetched: '2026-08-13'
  url: https://academy.chatfuel.com/_mcp/server
  method: POST tools/list
  http_status: 200
  content_type: text/event-stream
  tools_returned: 1
  auth_required: false
notes: >-
  No MCP server exists for the callable Chatfuel APIs (Contacts, Broadcasting,
  Dashboard). No candidate tool list was derived because Chatfuel publishes no
  OpenAPI to derive one from — deriving tools from prose would fabricate an
  inputSchema.
